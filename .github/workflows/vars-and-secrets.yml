name: vars-and-secrets-usage

on: push

jobs:
  vars-and-secrets:
    runs-on: ubuntu-latest
    env:
      DOCKER_REGISTRY: ttl.sh
    steps:
      - name: Default shell
        run: |
          env
      - name: Bash
        shell: bash
        run: |
          env
